# Git
.git
.gitignore

# Maven build output
**/target/

# IDE
.idea/
.vscode/
*.iml

# OS
.DS_Store

# Local secrets and environment files
.env
.env.*
!.env*.example
*.pem
*.key
*.p12
*.pfx
*.pkcs12
*.keystore
*.jks
id_rsa
id_rsa.*
id_ed25519
id_ed25519.*
id_ecdsa
id_ecdsa.*
credentials.json
secrets/
.aws/
# TLS material is mounted into the Nginx container at runtime and is never baked into an image.
# Excluded so the private key is not even uploaded to the build daemon.
infrastructure/nginx/certs/

# Docs and CI (not needed in runtime image)
docs/
.github/
*.md
!README.md

# Logs
logs/
*.log

# Test artifacts
**/surefire-reports/
**/failsafe-reports/
